_target_: egomimic.pl_utils.pl_data_utils.MultiDataModuleWrapper

train_datasets:
  dataset1:
    _target_: rldb.utils.RLDBDataset
    # Uncomment or adjust paths as needed:
    # parquet_path: /coc/flash9/skareer6/Projects/RLDB/rldb/export/oboo_robot
    repo_id: "rpuns/eve_laundry_rl2"
    root: "/coc/cedarp-dxu345-0/datasets/RLDB/robomimic_eve_laundry_debug/lerobot_debug"
    local_files_only: true
    mode: "train"

  dataset2:
    _target_: rldb.utils.RLDBDataset
    # Uncomment or adjust paths as needed:
    # parquet_path: /coc/flash9/skareer6/Projects/RLDB/rldb/export/oboo_robot
    repo_id: "rpuns/eve_laundry_rl2"
    root: "/coc/cedarp-dxu345-0/datasets/RLDB/robomimic_eve_laundry_debug/lerobot_debug"
    local_files_only: true
    mode: "train"

valid_datasets:
  dataset1:
    <<: ${train_datasets.dataset1}
    mode: "valid"     

  dataset2:
    <<: ${train_datasets.dataset2}
    mode: "valid"

train_dataloader_params:
  dataset1:
    batch_size: 32
    num_workers: 10
  dataset2:
    batch_size: 32
    num_workers: 10

valid_dataloader_params:
  dataset1:
    batch_size: 32
    num_workers: 10
  dataset2:
    batch_size: 32
    num_workers: 10